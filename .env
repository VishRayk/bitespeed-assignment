# Change the username (postgres) and password (password) if different on your machine.
# If you don't have a database named "bitespeed", Prisma will ask to create one when we run db push.
DATABASE_URL="postgresql://postgres:vishalr59@localhost:5432/bitespeed?schema=public"